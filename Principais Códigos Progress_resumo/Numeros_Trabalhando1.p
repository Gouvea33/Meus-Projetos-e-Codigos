/* Trabalhando com números */

// Progress ABL suporta três formatos de números: números inteiros e flutuantes de
32 e 64 bits.

/* Existem funções padrão integradas para comparar igualdade, desigualdade, etc. 
O símbolo pode ser trocado pelo alternativo e vice-versa. Então var1 <> var2 é a 
mesma coisa que var1 NE var2.

Você pode comparar um float com um número inteiro, mas não pode comparar, por 
exemplo, uma data com um número inteiro. */
